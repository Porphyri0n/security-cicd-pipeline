# Gelistirme ve test bagimliliklari
# Not: semgrep Windows'ta pip ile desteklenmez; WSL veya Docker kullanin
# (docker compose run semgrep-scanner). CI kendi adiminda kurar.
pytest==9.1.1
pytest-cov==7.0.0
